import UploadComponent from "../components/Upload"; // Use a different name for the imported component

const Upload = () => {
    return (
        <div>
            <UploadComponent /> {/* Use the renamed component here */}
        </div>
    );
};

export default Upload;
